/*
	eslint-disable

	import-x/unambiguous,
	unicorn/no-empty-file,
	unicorn/prevent-abbreviations
	--
	jsdoc
*/

/**
 * @import { Join } from "@pumpn/type-party"
 * @import { LootRegexFilePathExtensionLastLetterPermutation } from "./loot-regex-file-path-extension-last-letter.doc/_exports.js"
 */

/**
 * @typedef {(
 * `[${Join<LootRegexFilePathExtensionLastLetterPermutation, "">}]` |
 * `(${Join<LootRegexFilePathExtensionLastLetterPermutation, "|">})`
 * )} LootRegexFilePathExtensionLastLetter
 */
