/*
	eslint-disable

	import-x/unambiguous,
	unicorn/no-empty-file,
	unicorn/prevent-abbreviations
	--
	jsdoc
*/

/**
 * @import { UrlProtocol } from "./url.doc/_exports.js";
 */

/**
 * @typedef {`${UrlProtocol}://${string}.${string}`} Url
 */
